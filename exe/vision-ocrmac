#!/usr/bin/env ruby
# frozen_string_literal: true

require "vision_ocrmac"

if ARGV.empty?
  warn "usage: vision-ocrmac <image_path>"
  exit 1
end

path = ARGV[0]
unless File.exist?(path)
  warn "file not found: #{path}"
  exit 2
end

print VisionOcrmac.recognize(path)
